# training-project

Ubuntu

1) Config network
- check host name:
  -- hostname
  -- cat /etc/hostname 
    --- the hostnamectl command doesn't update /etc/hosts for you, so you'll need to edit that file yourself.
